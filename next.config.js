/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  swcMinify: true,
  darkmode: false,
  compiler: {
    styledComponents: true,
  },
};

module.exports = nextConfig;
